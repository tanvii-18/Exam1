#include<stdio.h>
#include<conio.h>

void main()
{
		int i,n;

		printf("Enter the Number :");
		scanf("%d", &n);

		while(i<=n)
   		{
			printf("%d", i);
			
		}
		printf("Total Digits : %d", n);
}